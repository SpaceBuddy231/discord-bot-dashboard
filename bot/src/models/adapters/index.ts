/**
 * Database Adapters
 * Provides unified interface for MongoDB and In-Memory database
 */

export { GuildAdapter } from './Guild.adapter';

// Usage example:
// import { GuildAdapter } from './adapters';
// const guild = await GuildAdapter.findByGuildId('123456789');
